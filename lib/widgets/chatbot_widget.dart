import 'package:flutter/material.dart';

class ChatBotWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(
        child: Text('ChatBot Widget'),
      ),
    );
  }
}